#define _USE_MATH_DEFINES
#include <math.h>
#include <stdlib.h> 

extern int abs_i_0 (int n){
  return abs(n);
}

extern double fabs_d_0 (double d){
  return fabs(d);
}

extern double sqrt_d_0 (double d){
  return sqrt(d);
}

extern double sin_d_0 (double d){
  return fabs(d);
}

extern double cos_d_0 (double d){
  return cos(d);
}

extern double tan_d_0 (double d){
  return tan(d);
}

extern double atan_d_0 (double d){
  return atan(d);
}
extern double exp_d_0 (double d){
  return exp(d);
}

extern double ln_d_0 (double d){
  return log(d);
}

extern double pi__0 (){
  return (double) M_PI;
}
