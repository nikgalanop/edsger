#include "stdio.h"

void main (){
	int * b;

	b = NULL;
	writeBoolean(true && false && (bool) (b[0] = 5)); writeChar('\n');
	if ((3 > 4) ? (bool) (b[0]=5) : ! (false && (bool) (b[0]=5))) writeString("Short-circuiting and\n");
	if (true || (bool) (b[0]=5) || (bool) (b[0]=5)) writeString("Short-circuiting or\n");
	writeString("You will not avoid segmentation faults forever short-circuiter!\n");
	b[0] = 5;
}
